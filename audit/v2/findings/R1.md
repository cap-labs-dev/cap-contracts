# WS-R1 — Regression of round-1 C-1, H-1, H-2, M-1..M-5 on `3dad5ef`

Every round-1 PoC was ported to the new API (real `Oracle` + 8-dec `MockAggregator`, `_setPrice`, `createFloatingMarket`, `optIn()`/`IPremiumVesting.claim`, role ids MARKET=4/REGISTRY=5/LIQUIDATOR=6) with assertions kept identical in meaning. PoCs live in `audit/v2/tests/scratch/R1/`. Run:

```
FOUNDRY_TEST=audit/v2/tests/scratch/R1 forge test --match-path 'audit/v2/tests/scratch/R1/*' -vv
```

(Default fuzz profile: invariant runs=256, depth=500. The `R1_H2_StablecoinQueueInvariants` control contract in the invariant file is O(n²) and does not finish at depth 500 in this environment; scope with `--match-contract R1_H2_TrancheQueueInvariants` or `FOUNDRY_INVARIANT_DEPTH=100`. It is not part of any finding.)

## Status table

| ID | Round-1 severity | Status on 3dad5ef | Evidence (test + real output) | Notes (what changed, residual) |
|---|---|---|---|---|
| C-1 oracle decimals | Critical | **FIXED** | `R1_C1_OracleDecimals.t.sol` — 5/5 PASS. `feed.answer()=200000000000` (8-dec), `oracle.price()=2000000000000000000000`, `oracle.DECIMALS()=18`; 1000 WETH → `totalCapital reported: 2000000000000000000000000` (= 2_000_000e18 exactly); `variableCreditLimit: 1000000000000000000000000`. Seize path at $1200 crash: `cUSD burned 6682027649769585253456`, `USD owed 6815668202764976958525`, `real USD seized 6815668202764976958000`, `WETH seized 5679723502304147465` of 10e18 (43% left). At $600 (insolvent, health 0.48): seized 6000e18 vs owed 5999999999999999999999 — whole tranche taken, which is now the correct outcome. | `Oracle.DECIMALS` 8→18, `ChainlinkAdapter` library normalises to 18, tests run on the real oracle. Liquidator pays exactly `repaid·(1+bonus)` to within 1 wei. No 1e10 discrepancy anywhere. Residual: none for this defect (oracle regressions L-15/N8 are WS-N4). |
| H-1 stale Underwriter mark | High | **OPEN** | `R1_H1_StaleMark.t.sol` — 0/2 PASS. `[FAIL: exiting depositor must not be paid above the true share price: 499999999999999998500 > 414999999999999998841] test_H1_exitAtStaleMarkAfterSlash` — `underwriter true assets: 829999999999999999340`, `reported totalAssets (stale): 999999999999999999000`, `alice actually paid: 499999999999999998500`, `bob value after report: 330000000000000000180`, `bob loss transferred from alice: 84999999999999998660`. `[FAIL: queued claim paid at stale mark: 499999999999999998500 > 414999999999999998841] test_H1_queuedExitAlsoAtStaleMark`. | `redeem/withdraw/requestRedeem` are still the untouched `ERC7540AsyncRedeem` paths; `_mark` reachable only from `_allocate/deallocate*/finalizeDeallocateAsync/_report`. New: `removeTranche` reports first, `addTranche` opts in, `_report` claims tranche premium into the underwriter pot, `lastReported` timestamp. None of it re-marks on exit; numbers differ from round 1 only by the 1e3 dead-share carve-out. Alice still walks with 85 tokens of Bob's. |
| H-2 queue out-of-order settlement | High | **OPEN** | `R1_H2_OutOfOrderSettlement.t.sol` — 0/3 PASS: `[FAIL: claimable must never exceed unlockedSupply: 99999999999999999000 > 0] test_B1_juniorClaimSucceedsWhileUnlockedSupplyIsZero` (`junior.unlockedSupply(): 0`, `junior.claimable(idA, alice): 99999999999999999000`, `lockedValue(junior): 714285714285714286429`); `[FAIL: next call did not revert as expected] test_B1_juniorClaimDrainsLockedTranche`; `[FAIL: a redemption must never make a healthy market liquidatable: 896000000000000000000000000 < 1000000000000000000000000000] test_B1_seniorExitFlipsHealthyMarketToLiquidatable` (health 1.12 → 0.896, `maxLiquidatable 407834101382488479263`). `R1_H2b_ControlAndUnderwriter.t.sol`: control PASS, `[FAIL: claimable must never exceed unlockedSupply: 99999999999999999000 > 0] test_B1_underwriterClaimableMisreportedAfterAllocate`. Fuzz `R1_H2_Queue.invariants.t.sol` (`R1_H2_TrancheQueueInvariants`, runs=256 depth=500): `invariant_I13_trancheQueueConservation` PASS `(runs: 256, calls: 128000, reverts: 0)`; `invariant_sumClaimable_le_unlocked` **FAIL** `[FAIL: sum claimable <= unlocked (tranche): 2933219010192558469669 > 2648470415591688786836] [Sequence] (original: 139, shrunk: 10)` — deposit 0.765e18 → deposit 1000e18 → price 1.5 → request 8388 shares (early, tiny) → request 349.9e18 → borrow ×3 → **claim on the later request** → price 0.795. Seed `0xb1069661026b5267d62006c02f040e4c756745ad2cbd9e904f54a3d76eeb6937`. | `claimableRedeemRequest` unchanged: `currentIndex = settledQueue + unlockedSupply()` and `_withdraw` does `settledQueue += _shares` for any request regardless of queue position, so a later claim advances every earlier request's window while `unlockedSupply()` moves exogenously (borrow/price/allocate). `ERC1155Queue` unchanged; `ERC165Upgradeable→ERC165` only. I17 still broken. |
| M-1 stale feed bricks liquidation | Medium | **OPEN** (failure mode changed) | `R1_M1_OracleBricksLiquidation.t.sol` — `[FAIL: InvalidPrice()] test_oneStaleFeedBricksLiquidationAndRedemption`: probes `market.healthiness: REVERT`, `maxLiquidatable: REVERT`, `unrecoverableDebt: REVERT`, `availableCredit: REVERT`, `writeOff (GUARDIAN): REVERT`, `senior.totalCapital (own feed fine): ok`, `senior.unlockedSupply: REVERT`, `senior.maxRedeem(alice): REVERT`, `junior.unlockedSupply: REVERT`, `market.repay: ok`; `liquidate(100e18)` on the crashed senior collateral reverts `InvalidPrice()`. PASS `test_staleFeed_oracleReturnsZero_trancheRevertsInvalidPrice`: `oracle.price(tokenB) (stale): 0`, `oracle.price(collateral): 300000000000000000`. | `Oracle._read` now returns 0 on staleness (no revert), but `Tranche.getPrice()` (`Tranche.sol:199-201`) reverts `ITranche.InvalidPrice()` on 0, and `BaseMarket.healthiness/variableCreditLimit` sum `totalCapital()` over all tranches. A 0.1%-of-capital junior tranche with a stale feed still blocks liquidation, write-off and senior redemption while the borrower can repay. Same severity. |
| M-2 EMA manipulation | Medium | **OPEN** (numbers changed) | `R1_M2_EmaManipulation.t.sol` — 2/5 PASS (analytic + holding-time logs), 3 FAIL: `[FAIL: a fully reversible deposit repriced a 30-day loan: 6778659191804016126482 != 8966376089663824195902]` (`discount (bps of honest): 2439`, `attacker capital cost at 10% APR: 114.155…`, `profit multiple: 19`); `[FAIL: profitable even at the longest permitted window: 36208587602102130735568 != 54794520547945205479452]` ($5M/30d, 1-day park of $10M: `lenders lose 18585.93`, `attacker cost 2739.73`, `profit multiple: 6`); `[FAIL: see log: profitable across the whole band]`. Holding-time table (period 1 h, $10M park vs $1M/30d @80% util): hold 300 s → `avg utilization 0.7408`, `discount 1046 bps`, `profit multiple 98`; hold 3600 s → `0.4901`, `2439 bps`, `19×`; hold 86400 s → `0.4000`, `2961 bps`, `0×`. Weight `1−r^t` (period 1 h): t=300 → `0.07997`, t=3600 → `0.63217`, t=86400 → `0.99999999996`. | Weight is now exponential (`rayPow`), so one period buys 63% of the full dilution instead of 100% and 5 min buys 8%. Attack unchanged: `fixedRatesAfterMint` still prices off `averageUtilizationAfterMint` only, no `max(avg, spot)` floor, park returns at par in the same block (`assertEq(redeemed, PARK)` held). Round-1 profit multiples 279×/23×/7.6× → now 98×/19×/<1× at $1M, and 6× at $5M for the 1-day window. Reduced at the margin, same severity. |
| M-3 JIT premium | Medium | **OPEN** (reduced) | `R1_M3_JitPremium.t.sol` — 0/3 PASS. `[FAIL: carol collected half a 30-day premium for 6 hours of exposure, then left: 1617069412303092062 != 0] test_JIT_fixedMarketUpfrontPremium`: `carol maxRedeem right after optIn: 1000000000000000000000`; premium 8.219 cUSD; carol take `1967 bps` at +6 h, `3160 bps` at +12 h, `4323 bps` at +24 h (alice identical at every horizon); `carol maxRedeem after claim: 1000000000000000000000`, `carol redeemed collateral: 1000000000000000000000` (100% of principal), `hours of term remaining, carried by alice alone: 714`. `[FAIL … 2866236610937238823 != 0] test_JIT_floatingChargePremium` (same 1967/3160/4323 bps). `[FAIL … 3314841198817717850 != 0] test_JIT_underwriterReportWindow`: carol (0 days exposure) `5000 bps`, `carol maxRedeem (underwriter shares): 0`. | 12 h exponential vest replaces 6 h linear; `optIn()` is instant (sets `debt = perShare·bal`, no delay/lock/entry checkpoint) and `_fund` credits the pot to *current* stakers. So a JIT depositor arriving one block before the premium is credited pro-rata exactly like the 30-day depositor; only the release curve slowed: 50% at +6 h (round 1) → 19.7% / 31.6% / 43.2% at 6/12/24 h, converging to 50%. Instant 100% redeem of principal still available (tranche path). Mechanism unfixed; magnitude per unit of JIT holding time roughly halved at 6 h. Stays Medium. |
| M-4 recognition lag | Medium | **OPEN** | `R1_M4_RecognitionLag.t.sol` — 4/4 PASS (the tests document the lag; identical numbers to round 1). `test_lagVsPrompt_halfTheReserveExitsAtPar`: LAG `A received 10000000000000000000000` vs PROMPT `8181818181818181818181`; survivor B `8000000000000000000000` vs `8348794063079777365491`; flat backing `0.8889` vs `0.9091`; `A + totalAssets (LAG−PROMPT): 0` (pure redistribution). `test_noPermissionlessRecognition`: `borrower drained reserve at par: 19999000000000000000000`. `test_writeOffIsPermanentForgiveness_transientDipLever`: `borrower forgiven 10000000000000000000000`, `badDebt still on cUSD holders 10000000000000000000000`. New `test_coverBadDebtPublic_doesNotRecognise` PASS: with the loan underwater and `badDebt==0`, `coverBadDebt` reverts `NoBadDebt` and A exits at par; after guardian `writeOff` (`written off 10000e18`) a rando's `coverBadDebt(1000e18)` reduces `badDebt` and `totalSupply` by 1000e18 with `totalAssets` unchanged. | `coverBadDebt` public (`Stablecoin.sol:156-167`) reads and only *decreases* `badDebt` by burning the caller's own cUSD; the only writer that increases it is still `recognizeBadDebt` (restricted) ← `BaseMarket._writeOff` ← `writeOff` (restricted). Making it public opens a donation path, not a recognition path. Lag unchanged. (Side note: since it burns 1:1 while redemptions are on the curve, it is strictly worse for the caller than redeeming — treasury/altruist hook only.) |
| M-5 loan-id reuse / phantom id | Medium | **OPEN** | `R1_M5_PhantomLoanId.t.sol` — `[FAIL: 30 days of exposure must cost the 30-day premium: 6535159817351598172 < 131506849315068493150] test_reTermViaPhantomId_paysMinimumTermForMaximumTerm` (`premium avoided: 124971689497716894978`, 95%). `R1_M5b_LoanIdReuse.t.sol` — 7/7 PASS (role-isolated quantification, identical to round 1): `test_quantify_10M_splitByRecipient`: `honest 30d liquidity premium 164383561643835616438356`, `paid 10776255707762557077625`; `honest underwriter premium 164383561643835616438356`, `paid 5479452054794520547945`; `avoided bps of honest total: 9505`; `test_repeatable_monthlyRollover`: `round 1 premium 16255707762557077625570`, `round 2 premium 8951459352482878816745` vs honest `222612015584943792672338`. | `FixedMarket.sol:64-118` unchanged: `borrow` does `id = loanCount++; expiry[id] = …` with no `expiry[id]==0 && debt[id]==0` assert; `borrowMore/extend/extendAdmin/repay/liquidate/writeOff` take a raw `id` with no `id < loanCount` check; `extend` on `expiry==0` still takes `_rollFromNow` with zero premium because `debt[id]==0`. Only port change: the liquidity-premium recipient is now `address(stablecoin)` (`fundCreditBacked`) instead of stcUSD. |

Summary: **1 FIXED (C-1), 7 OPEN (H-1, H-2, M-1, M-2, M-3, M-4, M-5)**. Two of the open ones changed shape without changing severity: M-1's failure now originates in `Tranche.getPrice` (`InvalidPrice`) rather than the oracle; M-2/M-3 are numerically reduced by the exponential curves but the mechanisms (no spot floor, instant opt-in credited at arrival) are untouched.

## Severity changes

### [CLOSED — was CRITICAL] C-1 Oracle answers 8 decimals, Tranche consumes 18
**Location:** contracts/cap/oracle/Oracle.sol (`DECIMALS = 18`), contracts/cap/oracle/ChainlinkAdapter.sol (library, normalises `10**(18-feedDecimals)`), contracts/cap/Tranche.sol:199-201 (`getPrice`)
**Impact:** none remaining. `totalCapital`, `creditLimit`, `lockedValue`, and the liquidation seize (`assets = value·1e18/price`) all read an 18-decimal USD price.
**Likelihood:** n/a — closed.
**Exploit path:** the round-1 path (dust debt → whole-tranche seizure at a 1e10-under price) no longer exists; at $1200 the liquidator takes 5.68 of 10 WETH = `repaid·1.02` USD to within 1 wei.
**Proof:** `audit/v2/tests/scratch/R1/R1_C1_OracleDecimals.t.sol` — 5/5 PASS (output in table).
**Recommendation:** keep `CapDeployer`'s deliberate 8-dec feeds so the suite continues to cross the adapter's normalisation; a feed with `decimals() > 18` and the deleted bounds check are WS-N4's (L-15/N8).
**Invariant broken:** none.

No other round-1 severity in this workstream's scope changes on `3dad5ef`. M-2 and M-3 are reduced in magnitude (see Notes) but retain a demonstrated, profitable, permissionless path and stay Medium; H-1/H-2 reproduce with round-1 numbers and stay High.

## Invariants

- **I17** (`Σ_open claimableRedeemRequest ≤ unlockedSupply()`): still broken on `Tranche` — `R1_H2_Queue.invariants.t.sol::invariant_sumClaimable_le_unlocked` shrinks to a 10-call counterexample on the default profile (2933e18 > 2648e18). Also broken on `Underwriter` deterministically (`R1_H2b`).
- **I13** (tranche queue conservation, `balanceOf(vault) == redemptionQueue`): holds — `invariant_I13_trancheQueueConservation` 256 runs / 128,000 calls / 0 reverts.
- **I23** (`Underwriter.totalDebt == Σ previewRedeem(balanceOf(t)+queuedShares[t])` at every redeem): broken as expected (`R1_H1`).
- New invariant the code implies and the plan missed: **`Stablecoin.badDebt` is monotone non-increasing under any non-`restricted` call** (only `recognizeBadDebt` may raise it). Held on this commit (`test_coverBadDebtPublic_doesNotRecognise`); worth carrying into the lead suite since `coverBadDebt`/`fund` are now public entry points touching the same accounting.
